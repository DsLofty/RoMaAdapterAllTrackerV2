# Checkpoints

Place the frozen AllTracker checkpoint here for a self-contained VOT package:

```text
alltracker.pth
```

The v2 runtime also accepts an external path through:

```bash
ALLTRACKER_CKPT=/path/to/alltracker.pth
```
